# rpp_dev
Development metapackage for rpp

# Installation
Run `setup.sh` script from the `rpp_dev` folder

# Getting started


Usefull commands:

`rpp pm` starts a Plugin Manager GUI

`rpp ws` starts a Workspace GUI

(NOTE): If using with ROS2, make sure you have sourced the environment in the terminal that runs GUI.
